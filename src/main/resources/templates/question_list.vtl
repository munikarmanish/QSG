<h1>Questions</h1>

#if ($questions.size() > 0)
  #foreach ($q in $questions)
    <p>$q.text</p>
  #end
#else
  <p>No questions</p>
#end

<!-- pagination -->
#if ($prevPage > 0)
  <a href="?page=$prevPage">Previous</a>
#end

Page $currentPage
<a href="?page=$nextPage">Next</a>
