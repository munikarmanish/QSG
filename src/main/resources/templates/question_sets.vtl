<h1>Sets</h1>
#if ($questions.size() > 0)
#set($nq=0)
 #if($questions.size()% 2 ==0)
 #set($nq= $questions.size() /3 )
 #else
 #set($nq= ($questions.size()+1) /3 )
 #end
 #set($i=0)
 #set($qid= [])
 #set($qnum= [])

  #foreach($set in [1..3])
    <p> Set $set </p>
      #set($cntq=0)
      #set($j=1)

      #foreach ($que in $questions)
        #if($cntq < $nq) 
        #set($i=$mathTool.random(1,$questions.size))
        #set($q= $questions[$i])
        #if($qid.add($q.getId()))#end
                          ##set($qid[$i]= $q.getId())**
                          ##<p>[$q.getId()] $q.getText()</p>**
        <p>[$j] $q.getText()</p>
        #if($qnum.add($j))#end
        <ul>
        #foreach ($a in $q.getAnswers())
          #if ($a.getIsCorrect())
          <li><em>$a.getText()</em></li>
          #else
          <li>$a.getText()</li>
          #end
        #end

        </ul>
        #else
        #break
        #end

      #set($i=$i+1)
      #set($j=$j+1)
      #set($cntq=$cntq+1)
      #end

  
    

  #end
   

#else
  <p>No questions</p>
#end

<form action="/save_set" method="post">
 <input type="hidden" name="questionid[]" value="$qid">
 <input type="hidden" name="questionnum[]" value="$qnum">

 <button type="submit" class="btn btn-primary btn-outline-success">Save</button>                 
</form>
